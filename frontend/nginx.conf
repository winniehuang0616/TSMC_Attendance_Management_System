server {
    listen 80;
    server_name _;
    
    # 處理前端資源
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
    
    # API 轉發配置不需要了，因為會由 Kubernetes Ingress 處理
    # 這裡我們移除了原先 nginx.conf 中的 proxy_pass 配置
}